{
  "name": "microscalper-pro",
  "env": "python",
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "python server.py",
  "envVars": [
    { "key": "CLIENT_ID", "sync": false },
    { "key": "FLATTRADE_API_KEY", "sync": false },
    { "key": "FLATTRADE_SECRET", "sync": false }
  ]
}
